# Bu dosya GIT'E GİRER — şablondur, gerçek değer YAZMA.
# Kurulumda kopyala:  cp .env.example .env.local   ve  DATABASE_URL'ü .env'e koy.

# --- .env içine (Prisma CLI burayı okur) ---
DATABASE_URL="postgresql://KULLANICI:SIFRE@HOST.neon.tech/DB?sslmode=require"

# --- .env.local içine (Next.js okur) ---
NEXT_PUBLIC_MENU_URL="https://take-away-system.vercel.app/api/products"
PUSHER_APP_ID="..."
NEXT_PUBLIC_PUSHER_KEY="..."
PUSHER_SECRET="..."
NEXT_PUBLIC_PUSHER_CLUSTER="eu"

# İleride (gün sonu ciro API'si için gizli token):
# RAPOR_TOKEN="uzun-rastgele-bir-anahtar"
